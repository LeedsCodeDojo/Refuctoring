#!/usr/bin/python3

# Stringify the number
# Over-indentation

def fizzbuzz(number):
	number = '.' * number

	if len(number) % 15 == 0:
		return 'FizzBuzz'
	else:
		if len(number) % 3 == 0:
			return 'Fizz'
		else:
			if len(number) % 5 == 0:
				return 'Buzz'
			else:
				return len(number)
	
if __name__ == '__main__':
	for i in range(30):
		print(fizzbuzz(i))
